{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AffiliateRegisterRequest",
  "type": "object",
  "required": ["tenant_id", "name"],
  "properties": {
    "tenant_id": {"type": "string", "format": "uuid"},
    "user_id": {"type": "string", "format": "uuid"},
    "name": {"type": "string"},
    "commission_rate": {"type": "number"}
  }
}

